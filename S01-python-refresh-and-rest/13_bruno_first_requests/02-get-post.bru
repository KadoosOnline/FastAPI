meta {
  name: Get one post
  type: http
  seq: 2
}

get {
  url: {{publicApi}}/posts/1
  body: none
  auth: none
}

tests {
  test("has id 1", function() {
    expect(res.body.id).to.equal(1);
  });
}
