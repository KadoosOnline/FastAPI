meta {
  name: List posts of user 1
  type: http
  seq: 1
}

get {
  url: {{publicApi}}/posts?userId=1
  body: none
  auth: none
}

params:query {
  userId: 1
}

tests {
  test("200 and a JSON array", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
