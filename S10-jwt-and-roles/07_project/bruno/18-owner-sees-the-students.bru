meta {
  name: Owner sees the students
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/courses/{{courseId}}/students
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(1);
  });
}
