meta {
  name: Me without a token (401)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/users/me
  body: none
  auth: none
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
}
