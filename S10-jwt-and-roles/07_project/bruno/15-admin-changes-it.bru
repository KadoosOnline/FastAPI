meta {
  name: Admin changes it
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/courses/{{courseId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "capacity": 3
  }
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
