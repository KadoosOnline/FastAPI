meta {
  name: Student lists users (403)
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
}
