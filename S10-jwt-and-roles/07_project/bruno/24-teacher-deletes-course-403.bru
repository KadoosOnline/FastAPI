meta {
  name: Teacher deletes course (403)
  type: http
  seq: 24
}

delete {
  url: {{baseUrl}}/courses/{{courseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

tests {
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
}
