meta {
  name: Courses taught by Ali
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/users/2/courses-taught
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(3);
  });
}
