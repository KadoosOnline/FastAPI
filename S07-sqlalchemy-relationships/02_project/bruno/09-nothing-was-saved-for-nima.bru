meta {
  name: Nothing was saved for Nima
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/users/{{nimaId}}/enrollments
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(0);
  });
}
