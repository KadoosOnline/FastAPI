meta {
  name: Students of FastAPI
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/courses/3/students
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(2);
  });
}
