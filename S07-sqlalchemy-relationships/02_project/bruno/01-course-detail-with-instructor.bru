meta {
  name: Course detail with instructor
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/courses/3
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.instructor.email).to.equal("teacher@kadoos.ir");
  });
}
