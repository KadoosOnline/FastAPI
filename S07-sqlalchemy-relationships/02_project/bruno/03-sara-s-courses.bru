meta {
  name: Sara's courses
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users/4/enrollments
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body[0].course).to.have.property("title");
  });
}
