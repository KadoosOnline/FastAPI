meta {
  name: Get course 1
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/courses/1
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
