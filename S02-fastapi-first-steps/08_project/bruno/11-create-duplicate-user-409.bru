meta {
  name: Create duplicate user (409)
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "email": "sara@example.com",
    "full_name": "Sara Again"
  }
}

tests {
  test("status is 409", function() {
    expect(res.status).to.equal(409);
  });
}
