meta {
  name: Create course
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/courses
  body: json
  auth: none
}

body:json {
  {
    "title": "Git",
    "price": 3000000,
    "capacity": 15
  }
}

tests {
  test("status is 201", function() {
    expect(res.status).to.equal(201);
  });
}
