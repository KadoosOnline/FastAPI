meta {
  name: Course id is not a number (422)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/courses/abc
  body: none
  auth: none
}

tests {
  test("status is 422", function() {
    expect(res.status).to.equal(422);
  });
}
