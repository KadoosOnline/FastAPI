meta {
  name: Delete course 3
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/courses/3
  body: none
  auth: none
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
