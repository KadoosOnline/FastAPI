meta {
  name: Price range
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/courses?min_price=2000000&max_price=3000000
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
