meta {
  name: Last page
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/courses?page=3&size=2
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.items.length).to.equal(1);
  });
}
