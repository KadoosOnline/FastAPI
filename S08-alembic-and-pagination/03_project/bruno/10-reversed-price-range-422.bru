meta {
  name: Reversed price range (422)
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/courses?min_price=5&max_price=1
  body: none
  auth: none
}

tests {
  test("status is 422", function() {
    expect(res.status).to.equal(422);
  });
}
