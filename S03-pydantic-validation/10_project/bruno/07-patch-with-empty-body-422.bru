meta {
  name: Patch with empty body (422)
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/courses/1
  body: json
  auth: none
}

body:json {
  {}
}

tests {
  test("status is 422", function() {
    expect(res.status).to.equal(422);
  });
}
