meta {
  name: Download without a token (401)
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/materials/{{materialId}}/download
  body: none
  auth: none
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
}
