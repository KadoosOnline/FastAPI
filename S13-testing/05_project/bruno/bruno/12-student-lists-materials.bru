meta {
  name: Student lists materials
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/courses/1/materials
  body: none
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(1);
  });
}
