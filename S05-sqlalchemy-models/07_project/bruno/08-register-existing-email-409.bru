meta {
  name: Register existing email (409)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "email": "sara@example.com",
    "full_name": "Sara"
  }
}

tests {
  test("status is 409", function() {
    expect(res.status).to.equal(409);
  });
}
