meta {
  name: List courses (from the database)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/courses
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
