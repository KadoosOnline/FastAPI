meta {
  name: Create course
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/courses
  body: json
  auth: none
}

headers {
  X-API-Key: {{adminKey}}
}

body:json {
  {
    "title": "Git",
    "price": 3000000,
    "capacity": 15,
    "instructor_id": 2
  }
}

tests {
  test("status is 201", function() {
    expect(res.status).to.equal(201);
  });
}
