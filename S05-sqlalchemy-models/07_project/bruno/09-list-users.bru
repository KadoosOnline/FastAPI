meta {
  name: List users
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: none
}

headers {
  X-API-Key: {{adminKey}}
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
