meta {
  name: List courses page 1
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/courses?page=1&size=1
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(1);
  });
}
