meta {
  name: Register same email (409)
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "email": "REZA@example.com",
    "full_name": "Reza Again"
  }
}

tests {
  test("status is 409", function() {
    expect(res.status).to.equal(409);
  });
}
