meta {
  name: Search courses
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/courses?q=fast
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
