meta {
  name: Create course without key (401)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/courses
  body: json
  auth: none
}

body:json {
  {
    "title": "Docker",
    "price": 3000000,
    "capacity": 15,
    "instructor_id": 1
  }
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
}
