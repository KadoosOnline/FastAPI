meta {
  name: Admin disables Sara
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/users/4
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "is_active": false
  }
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
