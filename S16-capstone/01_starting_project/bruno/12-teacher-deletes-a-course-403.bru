meta {
  name: Teacher deletes a course (403)
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/courses/3
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

tests {
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
}
