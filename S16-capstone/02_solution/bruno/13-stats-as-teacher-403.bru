meta {
  name: Stats as teacher (403)
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/users/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

tests {
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
}
