meta {
  name: Student enrolls in FastAPI
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/courses/3/enrollments
  body: none
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

tests {
  test("status is 201", function() {
    expect(res.status).to.equal(201);
  });
}
