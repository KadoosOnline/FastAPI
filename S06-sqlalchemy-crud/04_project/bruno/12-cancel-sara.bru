meta {
  name: Cancel Sara
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/courses/{{courseId}}/enrollments/4
  body: none
  auth: none
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
