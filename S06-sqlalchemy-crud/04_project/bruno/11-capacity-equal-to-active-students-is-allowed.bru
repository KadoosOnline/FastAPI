meta {
  name: Capacity equal to active students is allowed
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/courses/{{courseId}}
  body: json
  auth: none
}

headers {
  X-API-Key: {{adminKey}}
}

body:json {
  {
    "capacity": 1
  }
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
