meta {
  name: Search python courses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/courses?q=python
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.length).to.equal(2);
  });
}
