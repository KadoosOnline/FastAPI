meta {
  name: Patch with null (422)
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/courses/{{courseId}}
  body: json
  auth: none
}

headers {
  X-API-Key: {{adminKey}}
}

body:json {
  {
    "title": null
  }
}

tests {
  test("status is 422", function() {
    expect(res.status).to.equal(422);
  });
}
