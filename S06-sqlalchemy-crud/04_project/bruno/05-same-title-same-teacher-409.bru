meta {
  name: Same title same teacher (409)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/courses
  body: json
  auth: none
}

headers {
  X-API-Key: {{adminKey}}
}

body:json {
  {
    "title": "Git",
    "price": 1,
    "capacity": 1,
    "instructor_id": 2
  }
}

tests {
  test("status is 409", function() {
    expect(res.status).to.equal(409);
  });
}
